version: '3'

services:
  app1:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: always

  app2:
    build:
      context: .
      dockerfile: Dockerfile2
    ports:
      - "4000:3000"
    environment:
      - PORT=3000
    restart: always
